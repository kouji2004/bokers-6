<!--id="comments_areaにをrenderで指定した部分テンプレートに更新-->
$("#comments_area").html("<%= j(render 'book_comments/book_comments', book: @book) %>");
<!--フォームを送信したあとにフォームの値を空にします-->
$("textarea").val('');
$("#count_area").html("<%= j(render 'book_comments/book_count', book: @book) %>");
